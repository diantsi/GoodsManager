<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:GoodsManager.ViewModels"
             xmlns:mct="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="GoodsManager.Pages.GoodCreatePage"
             x:DataType="vm:GoodCreateViewModel"
             Title="{Binding Title}">

    <ContentPage.Resources>
        <mct:IsStringNotNullOrWhiteSpaceConverter x:Key="IsStringNotNullOrWhiteSpaceConverter" />
    </ContentPage.Resources>

    <Grid>
        <ScrollView>
            <VerticalStackLayout Padding="20" Spacing="15">
                
                <Label Text="Назва товару" FontAttributes="Bold" />
                <Entry Text="{Binding GoodTitle}" Placeholder="Введіть назву..." />
                <Label Text="{Binding Errors[GoodTitle]}" 
                       TextColor="Red" 
                       FontSize="Small"
                       IsVisible="{Binding Errors[GoodTitle], Converter={StaticResource IsStringNotNullOrWhiteSpaceConverter}}" />

                <Label Text="Категорія" FontAttributes="Bold" />
                <Picker ItemsSource="{Binding Categories}"
                        SelectedItem="{Binding SelectedCategory}" />

                <Label Text="Кількість" FontAttributes="Bold" />
                <Entry Text="{Binding QuantityText}" Keyboard="Numeric" />
                <Label Text="{Binding Errors[QuantityText]}" 
                       TextColor="Red" 
                       FontSize="Small"
                       IsVisible="{Binding Errors[QuantityText], Converter={StaticResource IsStringNotNullOrWhiteSpaceConverter}}" />

                <Label Text="Ціна" FontAttributes="Bold" />
                <Entry Text="{Binding PriceText}" Keyboard="Numeric" />
                <Label Text="{Binding Errors[PriceText]}" 
                       TextColor="Red" 
                       FontSize="Small"
                       IsVisible="{Binding Errors[PriceText], Converter={StaticResource IsStringNotNullOrWhiteSpaceConverter}}" />

                <Label Text="Опис" FontAttributes="Bold" />
                <Editor Text="{Binding Description}" Placeholder="Введіть опис..." HeightRequest="100" />

                <Grid ColumnDefinitions="*, *" ColumnSpacing="10" Margin="0,20,0,0">
                    <Button Grid.Column="0" 
                            Text="{Binding SubmitButtonText}" 
                            Command="{Binding SaveCommand}" 
                            BackgroundColor="#2196F3" />
                    <Button Grid.Column="1" 
                            Text="Скасувати" 
                            Command="{Binding CancelCommand}" 
                            BackgroundColor="Gray" />
                </Grid>

            </VerticalStackLayout>
        </ScrollView>

        <!-- Loading Overlay -->
        <Grid BackgroundColor="#80000000"
              IsVisible="{Binding IsBusy}">
            <VerticalStackLayout VerticalOptions="Center"
                                 HorizontalOptions="Center"
                                 Spacing="15">
                <ActivityIndicator IsRunning="{Binding IsBusy}"
                                   Color="Magenta"
                                   HeightRequest="50"
                                   WidthRequest="50" />
                <Label Text="Завантаження..."
                       TextColor="White"
                       FontSize="18"
                       FontAttributes="Bold" />
            </VerticalStackLayout>
        </Grid>
    </Grid>
</ContentPage>
